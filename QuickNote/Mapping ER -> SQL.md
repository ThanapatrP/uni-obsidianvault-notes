Design lvl (DataDict) :  tell data types,  not null, candidate key

**cardinallity** - near side (relation) doesn't need "unique" property (cardinallity)

**null / not null** - far side -> doenst need "not null"

// near side -> "near optional symbol side"

## which side to place FK
#### 1-1
both mandatory -> pick 1 side
one optional -> side that ISNT optional
both optional -> both side

#### M-N
both optional -> pick 1 side


